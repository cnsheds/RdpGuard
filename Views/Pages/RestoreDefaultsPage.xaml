<Page x:Class="OpenRdpGuard.Views.Pages.RestoreDefaultsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:OpenRdpGuard.ViewModels"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance vm:RestoreDefaultsViewModel, IsDesignTimeCreatable=True}">

    <ScrollViewer>
        <StackPanel Margin="12">
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="恢复默认" Style="{StaticResource PageTitleTextStyle}" Margin="0,0,0,8" />
                    <TextBlock Text="将恢复默认端口 3389，关闭白名单并清空黑名单。" Style="{StaticResource MutedTextStyle}" Margin="0,0,0,12" />
                    <Button Content="恢复默认设置" Width="140" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding RestoreCommand}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
