<Page x:Class="OpenRdpGuard.Views.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      xmlns:vm="clr-namespace:OpenRdpGuard.ViewModels"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance vm:SettingsViewModel, IsDesignTimeCreatable=True}"
      Title="Settings">

    <StackPanel Margin="24">
        <TextBlock Text="Settings" Style="{StaticResource HeaderTextBlockStyle}" Margin="0,0,0,24"/>

        <!-- RDP Port Configuration -->
        <ui:SimpleStackPanel Spacing="10" Margin="0,0,0,24">
            <TextBlock Text="RDP Port Configuration" Style="{StaticResource SubtitleTextBlockStyle}"/>
            <TextBlock Text="Change the listening port for Remote Desktop. Standard is 3389." Foreground="Gray"/>
            
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <TextBox Text="{Binding RdpPort}" Width="150" ui:ControlHelper.Header="Port Number"/>
                <Button Content="Save &amp; Update Firewall" Command="{Binding SavePortCommand}" VerticalAlignment="Bottom" Margin="10,0,0,0"/>
            </StackPanel>
        </ui:SimpleStackPanel>

        <ui:SimpleStackPanel Spacing="10" Margin="0,0,0,24">
            <TextBlock Text="Service Management" Style="{StaticResource SubtitleTextBlockStyle}"/>
            <Button Content="Restart RDP Service" Command="{Binding RestartRdpCommand}"/>
        </ui:SimpleStackPanel>

        <ui:SimpleStackPanel Spacing="10">
            <TextBlock Text="About" Style="{StaticResource SubtitleTextBlockStyle}"/>
            <TextBlock Text="{Binding AppVersion, StringFormat='OpenRdpGuard v{0}'}"/>
            <TextBlock Text="Secure your Windows Server RDP access." Foreground="Gray"/>
        </ui:SimpleStackPanel>
    </StackPanel>
</Page>