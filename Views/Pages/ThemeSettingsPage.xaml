<Page x:Class="OpenRdpGuard.Views.Pages.ThemeSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:OpenRdpGuard.ViewModels"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance vm:ThemeSettingsViewModel, IsDesignTimeCreatable=True}">

    <ScrollViewer>
        <StackPanel Margin="12">
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="主题设置" Style="{StaticResource PageTitleTextStyle}" Margin="0,0,0,8" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="当前主题: " Foreground="{DynamicResource SecondaryTextBrush}" />
                        <TextBlock Text="{Binding CurrentThemeText}" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="浅色模式" Width="100" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SetLightCommand}" />
                        <Button Content="深色模式" Width="100" Margin="8,0,0,0" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SetDarkCommand}" />
                        <Button Content="跟随系统" Width="100" Margin="8,0,0,0" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SetSystemCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
